/* ==========================================================
   TRADING JOURNAL PRO
   PROPFIRM
   RULES.JS
========================================================== */

/**
 * ==========================================================
 * REGRAS PADRÃO
 * ==========================================================
 *
 * Estas regras representam uma configuração padrão
 * para contas de avaliação (Prop Firms).
 *
 * Futuramente poderão ser carregadas de presets
 * específicos (Apex, TopStep, Take Profit Trader,
 * MyFundedFutures etc.).
 */

export const PropFirmRules = {

    /* ======================================================
       META DE LUCRO
    ====================================================== */

    profitTarget: 3000,

    /* ======================================================
       LIMITE DE PERDA DIÁRIA
    ====================================================== */

    dailyLossLimit: -1000,

    /* ======================================================
       DRAWDOWN MÁXIMO
    ====================================================== */

    maxDrawdown: -2500,

    /* ======================================================
       DIAS MÍNIMOS OPERADOS
    ====================================================== */

    minimumTradingDays: 5,

    /* ======================================================
       REGRA DE CONSISTÊNCIA
    ====================================================== */

    consistencyEnabled: true,

    consistencyLimit: 40,

    /* ======================================================
       RESET AUTOMÁTICO
    ====================================================== */

    trailingDrawdown: true,

    /* ======================================================
       COMISSÃO POR CONTRATO
    ====================================================== */

    commissionPerContract: 0,

    /* ======================================================
       SLIPPAGE
    ====================================================== */

    slippagePerTrade: 0,

    /* ======================================================
       HORÁRIO PERMITIDO
    ====================================================== */

    session: {

        enabled: false,

        start: "00:00",

        end: "23:59"

    }

};

/* ==========================================================
   PRESETS FUTUROS
========================================================== */

export const PropFirmPresets = {

    DEFAULT: PropFirmRules

};